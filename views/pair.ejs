<!DOCTYPE html>
<html>
<head>
  <title>SHUKRANI - WhatsApp Pairing</title>
  <style>
    body { font-family: sans-serif; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100vh; }
    img { width: 300px; height: 300px; }
    code { font-size: 1.5em; background: #f1f1f1; padding: 8px; border-radius: 8px; }
    a.button { background: #4CAF50; color: white; padding: 10px 20px; margin-top: 20px; border-radius: 8px; text-decoration: none; }
  </style>
</head>
<body>
  <h2>🤳 Scan this QR to pair your WhatsApp</h2>
  <% if (qrImage) { %>
    <img src="<%= qrImage %>" alt="QR Code" />
  <% } else { %>
    <p>⏳ Loading QR Code...</p>
  <% } %>

  <% if (pairingCode) { %>
    <p>🔢 Pairing Code:</p>
    <code><%= pairingCode %></code>
    <a class="button" href="/download/<%= sessionID %>">⬇ Download Session</a>
  <% } else { %>
    <p>🔄 Waiting for pairing code...</p>
  <% } %>
</body>
</html>
